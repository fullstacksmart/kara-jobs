import React from 'react';
import styles from './JobSearch.module.scss';

const JobSearch: React.FC<unknown> = (props: unknown) => {
  return (
    <div className={styles.JobSearch}>
      <h1>Signed in</h1>
    </div>
  );
};

export default JobSearch;
