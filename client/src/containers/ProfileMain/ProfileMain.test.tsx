export {};
import React from 'react';
import { render } from '@testing-library/react';

// import ProfileMain from './ProfileMain';
describe('ProfileMain', () => {
  it('renders ProfileMain', () => {
    render(<div></div>);
    // render(<ProfileMain />);
    // expect(screen.getByText('')).not.toBe(null);
  });
});
