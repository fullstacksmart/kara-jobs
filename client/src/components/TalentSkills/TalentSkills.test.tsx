import React from 'react';
import { render } from '@testing-library/react';

// import TalentSkills from './TalentSkills';
describe('TalentSkills', () => {
  it('renders TalentSkills', () => {
    render(<div></div>);
    // render(<TalentSkills />);
    // expect(screen.getByText('')).not.toBe(null);
  });
});
