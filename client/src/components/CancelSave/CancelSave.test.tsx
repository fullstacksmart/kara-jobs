import React from 'react';
import { render } from '@testing-library/react';

// import CancelSave from './CancelSave';
describe('CancelSave', () => {
  it('renders CancelSave', () => {
    render(<div></div>);
    // render(<CancelSave />);
    // expect(screen.getByText('')).not.toBe(null);
  });
});
