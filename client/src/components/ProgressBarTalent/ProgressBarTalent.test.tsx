import React from 'react';
import { render, screen } from '@testing-library/react';

// import ProgressBarTalent from './ProgressBar';
// describe('ProgressBarTalent', () => {
//   it('renders ProgressBarTalent', () => {
//     render(<ProgressBarTalent />);
//     expect(screen.getByText('ProgressBarTalent works!')).not.toBe(null);
//   })
// })
