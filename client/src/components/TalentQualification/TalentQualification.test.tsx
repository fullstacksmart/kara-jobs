import React from 'react';
import { render } from '@testing-library/react';

// import TalentQualification from './TalentQualification';
describe('TalentQualification', () => {
  it('renders TalentQualification', () => {
    render(<div></div>);
    // render(<TalentQualification />);
    // expect(screen.getByText('')).not.toBe(null);
  });
});
