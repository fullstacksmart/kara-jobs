import React from 'react';
import { render } from '@testing-library/react';

// import TalentSingleExperience from './TalentSingleExperience';
describe('TalentSingleExperience', () => {
  it('renders TalentSingleExperience', () => {
    render(<div></div>);
    // render(<TalentSingleExperience />);
    // expect(screen.getByText('')).not.toBe(null);
  });
});
