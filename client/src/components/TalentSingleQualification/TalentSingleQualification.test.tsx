import React from 'react';
import { render } from '@testing-library/react';

// import TalentSingleQualification from './TalentSingleQualification';
describe('TalentSingleQualification', () => {
  it('renders TalentSingleQualification', () => {
    render(<div></div>);
    // render(<TalentSingleQualification />);
    // expect(screen.getByText('')).not.toBe(null);
  });
});
