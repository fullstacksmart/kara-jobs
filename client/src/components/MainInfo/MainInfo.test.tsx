export {};
import React from 'react';
import { render } from '@testing-library/react';

// import MainInfo from './MainInfo';
describe('MainInfo', () => {
  it('renders MainInfo', () => {
    render(<div></div>);
    // render(<MainInfo name="" profession="" city="" />);
    // expect(screen.getByText('')).not.toBe(null);
  });
});
