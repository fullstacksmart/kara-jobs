import React from 'react';
import { render } from '@testing-library/react';

// import TalentExperience from './TalentExperience';
describe('TalentExperience', () => {
  it('renders TalentExperience', () => {
    render(<div></div>);
    // render(<TalentExperience />);
    // expect(screen.getByText('')).not.toBe(null);
  });
});
