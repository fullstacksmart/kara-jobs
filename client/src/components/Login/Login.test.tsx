import React from 'react';
import { render } from '@testing-library/react';

// import Login from './Login';
describe('Login', () => {
  it('renders Login', () => {
    render(<div>tesst</div>);
    //expect(screen.getByText('Login works!')).not.toBe(null);
  });
});
