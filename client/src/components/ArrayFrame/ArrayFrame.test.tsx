import React from 'react';
import { render } from '@testing-library/react';

// import ArrayFrame from './ArrayFrame';
describe('ArrayFrame', () => {
  it('renders ArrayFrame', () => {
    render(<div></div>);
    // render(<ArrayFrame />);
    // expect(screen.getByText('')).not.toBe(null);
  });
});
