import React from 'react';
import { render } from '@testing-library/react';

// import ExperienceAdder from './ExperienceAdder';
describe('ExperienceAdder', () => {
  it('renders ExperienceAdder', () => {
    render(<div></div>);
    // render(<ExperienceAdder />);
    // expect(screen.getByText('')).not.toBe(null);
  });
});
