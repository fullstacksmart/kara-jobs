import React from 'react';
import { render } from '@testing-library/react';

// import EditableList from './EditableList';
describe('EditableList', () => {
  it('renders EditableList', () => {
    render(<div></div>);
    // render(<EditableList />);
    // expect(screen.getByText('')).not.toBe(null);
  });
});
