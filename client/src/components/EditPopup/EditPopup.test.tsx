import React from 'react';
import { render } from '@testing-library/react';

// import EditPopup from './EditPopup';
describe('EditPopup', () => {
  it('renders EditPopup', () => {
    render(<div></div>);
    // render(<EditPopup />);
    // expect(screen.getByText('')).not.toBe(null);
  });
});
